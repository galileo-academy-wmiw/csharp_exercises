public class Implementation
{
    public string GetUserInput(string input)
    {
        // TODO: create a string variable that uses the input parameter to greet the user
        string interpolatedValue = ""; 

        return interpolatedValue;
    }

    public int Add(int a, int b)
    {
        // TODO: create a variable named sum and assign it the sum of a and b
        int sum = -255; 

        return sum;
    }

    public int Subtract(int a, int b) 
    {
        // TODO: create a variable named difference and assign it the difference of a and b
        int difference = -255;

        return difference;
    }

    public int Modulo(int a, int b) 
    {
        // TODO: create a variable named remainder and assign it the remainder of a divided by b
        int remainder = -255;

        return remainder;
    }

    public double GetAbsoluteValue(double num)
    {
        // TODO: create a variable named absoluteValue and assign it the absolute value of num
        double absoluteValue = -255;

        return absoluteValue;
    }

    public double GetSquareRoot(double num)
    {
        // TODO: create a variable named squareRoot and assign it the square root of num
        double squareRoot = -255;

        return squareRoot;
    }

    public double GetFlooredValue(double num)
    {
        // TODO: create a variable named flooredValue and assign it the floored value of num
        double flooredValue = -255;

        return flooredValue;
    }

    public double GetMinimum(double a, double b)
    {
        // TODO: create a variable named minimum and assign it the minimum of a and b
        double minimum = -255;

        return minimum;
    }

    public string FormatString(string name, int age)
    {
        // TODO: create a variable named formattedString and assign it a string that formats the name and age, eg. "Naam: Bob, Leeftijd: 30"
        string formattedString = "";

        return formattedString;
    }

    public int OperatorShortcuts(int value)
    {
        /**
        TODO:

        Increment the value by 1 using the increment operator

        Multiply the value by 2 using the multiplication operator

        Subtract 3 from the value using the subtraction operator

        Calculate the remainder of the value divided by 5 using the modulo operator
        */

        return value;
    }
}
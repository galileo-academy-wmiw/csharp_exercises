public class Implementation
{
    public string GetUserInput(string input)
    {
        string interpolatedValue = // TODO: create a string variable that uses the input parameter to greet the user

        return interpolatedValue;
    }

    public int Add(int a, int b)
    {
        int sum = // TODO: create a variable named sum and assign it the sum of a and b

        return sum;
    }

    public int Subtract(int a, int b) 
    {
        int difference = // TODO: create a variable named difference and assign it the difference of a and b

        return difference;
    }

    public int Modulo(int a, int b) 
    {
        int remainder = // TODO: create a variable named remainder and assign it the remainder of a divided by b

        return remainder;
    }

    public double GetAbsoluteValue(double num)
    {
        double absoluteValue = // TODO: create a variable named absoluteValue and assign it the absolute value of num

        return absoluteValue;
    }

    public double GetSquareRoot(double num)
    {
        double squareRoot = // TODO: create a variable named squareRoot and assign it the square root of num

        return squareRoot;
    }

    public double GetFlooredValue(double num)
    {
        double flooredValue = // TODO: create a variable named flooredValue and assign it the floored value of num

        return flooredValue;
    }

    public double GetMinimum(double a, double b)
    {
        double minimum = // TODO: create a variable named minimum and assign it the minimum of a and b

        return minimum;
    }

    public string FormatString(string name, int age)
    {
        string formattedString = // TODO: create a variable named formattedString and assign it a string that formats the name and age, eg. "Naam: Bob, Leeftijd: 30"

        return formattedString;
    }

    public int OperatorShortcuts(int value)
    {
        /**
        TODO:

        Increment the value by 1 using the increment operator

        Multiply the value by 2 using the multiplication operator

        Subtract 3 from the value using the subtraction operator

        Calculate the remainder of the value divided by 5 using the modulo operator
        */

        return value;
    }
}
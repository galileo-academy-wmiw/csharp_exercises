public class Implementation
{
    public string ConvertToLower(string input)
    {
        // TODO: create a variable named lowerCase and assign it the value of input converted to lowercase
        var lowerCase = "";

        return lowerCase;
    }

    public string ConvertToUpper(string input)
    {
        // TODO: create a variable named upperCase and assign it the value of input converted to uppercase
        var upperCase = "";

        return upperCase;
    }

    public int FindIndex(string input, char character)
    {
        // TODO: create a variable named index and assign it the index of the first occurrence of character in input
        var index = -255;

        return index;
    }

    public string GetFirstAndLastChar(string input)
    {
        // TODO: create a variable named firstAndLastChar and assign it the first and last characters of input, return nothing if the input is empty
        var firstAndLastChar = "TODO";

        return firstAndLastChar;
    }

    public string GetSubstring(string input, int startIndex)
    {
        // TODO: create a variable named substring and assign it the substring of input starting from startIndex
        var substring = "INVALID_SUBSTRING";

        return substring;
    }


}
